version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:./data/db.sqlite
    volumes:
      - ./prisma/data:/app/prisma/data
    command: npm run docker-start